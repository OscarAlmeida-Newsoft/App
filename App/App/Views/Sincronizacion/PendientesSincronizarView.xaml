<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="App.Views.Sincronizacion.PendientesSincronizarView"
             xmlns:local="clr-namespace:App.CustomCells;assembly=App">
  <StackLayout Spacing="20" Padding="0" >
    <ActivityIndicator x:Name="activityIndicator"  />
    <ListView x:Name="lvPendientesSincronizar"></ListView>
    <Button Text="Sincronizar" Clicked="btnSincronizar_Clicked"></Button>
  </StackLayout>

  <!--<StackLayout>
    <Label Text="Xamarin.Forms native cell" HorizontalTextAlignment="Center" />
    <ActivityIndicator x:Name="activityIndicator"  />
    <ListView x:Name="lvMenuEventos">
      <ListView.ItemTemplate>
        <DataTemplate>
          <local:PendienteSincronizarCustomCell NombreTipoEvento="{Binding NombreTipoEvento}" FechaEvento="{Binding FechaEvento}" NumeroManifiesto="{Binding NumeroManifiesto}" />
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </StackLayout>-->
</ContentPage>